#include <LordEngine.h>

namespace App
{
	/**
	* CLASS_NAME
	*/
	class CLASS_NAME : public LORD::IBehavior
	{
	public:
		// Use this for initialization
		virtual void start()
		{

		}

		// Tick is called once per frame
		virtual void tick(float deltaTime)
		{

		}
	};

	CLASS_REGISTER(CLASS_NAME)
}