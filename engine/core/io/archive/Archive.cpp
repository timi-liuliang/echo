#include "Archive.h"

namespace Echo
{
	// create
	Archive* Archive::create(const String& type)
	{
		//if (type == "filesystem")	return EchoNew(FileSystemArchive);

		return nullptr;
	}


	// load
	Archive* Archive::load(const String& name, const String& type)
	{
		return nullptr;
	}
}