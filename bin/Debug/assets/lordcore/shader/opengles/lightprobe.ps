#version 100

varying mediump vec3 normal;

void main(void)
{
//	mediump vec4 textureColor = texture2D(texSampler, texCoord);
	mediump vec4 color = vec4( normal * 0.0001 , 0.0 );
	gl_FragColor = vec4(1.0, 1.0, 0.0, 1.0 ) + color;
}