#version 100

uniform sampler2D texSampler;
uniform mediump vec4 color;

varying mediump vec2 texCoord;

void main(void)
{
	mediump vec4 textureColor = texture2D(texSampler, texCoord);

	if(textureColor.w < 0.32)
	{
		discard;
	}

	gl_FragColor = color;
}